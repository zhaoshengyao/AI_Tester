# Test Environment Configuration
# 
# ⚠️ DEPRECATED: This file is kept for backward compatibility only.
# ⚠️ Priority: Environment Variables (.env) > This File
# ⚠️ All new configurations should be added to .env file.
#
# Environment variables will override values in this file:
# - BASE_URL → test.base_url
# - API_BASE_URL → test.api_base_url
# - TEST_USERNAME → test.username
# - TEST_PASSWORD → test.password

test:
  base_url: ${BASE_URL:-http://192.168.2.97:6089}
  api_base_url: ${API_BASE_URL:-http://192.168.2.97:6089/prod-api}
  timeout: 30
  username: ${TEST_USERNAME:-ZhaoShengYao}
  password: ${TEST_PASSWORD:-123456}

staging:
  base_url: ${BASE_URL:-http://192.168.2.97:6089}
  api_base_url: ${API_BASE_URL:-http://192.168.2.97:6089/prod-api}
  timeout: 30
  username: ${TEST_USERNAME:-ZhaoShengYao}
  password: ${TEST_PASSWORD:-123456}

production:
  base_url: ${BASE_URL:-http://localhost:8080}
  api_base_url: ${API_BASE_URL:-http://localhost:8080/prod-api}
  timeout: 30
  username: ${TEST_USERNAME:-ZhaoShengYao}
  password: ${TEST_PASSWORD:-123456}

[pytest]
markers =
    smoke: Smoke tests - run on every build
    regression: Regression tests
    crud: CRUD operation tests
    full: Full test suite
addopts = --junitxml=reports/junit/report.xml --html=reports/html/report.html -v --tb=short
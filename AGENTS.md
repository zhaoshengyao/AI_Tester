# AGENTS.md

## 目标
本项目用于管理从需求分析到系统测试报告、再到测试知识沉淀的完整测试流程。

本项目的核心资产不是某一次测试结果，而是一套可复用的测试流程配置。配置必须说明：
- 每个阶段为什么要做。
- 每个阶段怎么做。
- 每个阶段基于什么证据决定下一步。
- 冒烟执行后如何判断继续、复测、补资料、降级执行或停止。
- 如何让同一套配置迁移到其他电脑、其他被测系统或其他 AI 模型后仍可复用。

## AI 读取优化
- AI 默认先读 `docs/AI执行最小规则.md`，再进入当前阶段的正式输入。
- 机器可读阶段契约以 `stage-manifests/*.yaml` 为唯一真源；`AGENTS.md`、`docs/阶段执行检查清单.md` 和各 `SKILL.md` 负责解释规则，不重复维护另一套代码判定口径。
- AI 驱动流程默认先读"轻量索引/摘要文件"，再按需展开具体报告、原始结果或脚本。
- 不得把整目录扫描当成默认输入；只有索引无法回答问题、存在统计冲突、或需要追证时，才读取更深层文件。
- 需求分析阶段默认先读 `docs/knowledge/README.md`，不默认整包读取 `docs/knowledge/*.md`。
- 系统测试报告阶段默认优先读取本轮 `docs/test-runs/YYYYMMDD-HHMMSS/批次摘要.md`、覆盖矩阵、专项报告和缺陷清单；只有证据不足或数据冲突时才展开原始结果。
- 测试知识沉淀阶段默认优先读取系统测试报告、本轮批次摘要、覆盖矩阵和缺陷清单，不默认整包读取 `tests/` 或历史批次归档。
- 同一阶段内已经形成的"最新摘要/索引/矩阵/报告"应作为下游阶段的主输入，避免后续阶段重新回读需求、原型、接口和所有原始结果。
- 需要长期保留的信息优先沉淀为索引、矩阵、批次摘要和模式库，而不是在多个长文档中重复展开。
- `批次摘要.md` 负责承接当前批次事实快照，`系统测试报告.md` 负责全局判断和证据归并，`docs/knowledge/*.md` 负责沉淀可复用规则与资产，三者不得互相整段重复。
- `docs/reports/reference/*阶段细化方案.md` 属于参考资料，不作为默认阶段输入；只有当前 skill 或正式文档不能回答问题时才按需展开。

## 流程约束
- 必须按阶段执行，不允许跳步骤。
- 需求分析阶段：若只有访问地址（无需求文档、接口文档），必须先登录系统成功并访问所有页面后，才能开始进行需求分析；登录失败时必须排查失败原因，禁止基于猜测编写分析文档。
- 代码侧阶段完成、阻塞、需更新、可进入下一步的机器判定，以 `scripts/check-stage.ps1` / `scripts/check-stage.sh` 消费 `stage-manifests/*.yaml` 的结果为准。
- 上一阶段输出未生成并保存，不得进入下一阶段。
- 所有结论必须基于已读取文档或已执行结果。
- 未执行的内容必须明确写"未执行"。
- 不确定的需求、规则、环境信息必须标记"待确认"，禁止猜测。
- 只要存在可访问的真实系统，接口分析、接口用例和接口自动化必须以真实接口行为为最高优先级；接口文档、需求文档和原型用于对照差异，不得覆盖真实实现。
- 有接口文档时，实际接口与文档不一致必须在需求分析报告、接口用例来源依据、接口自动化报告和系统测试报告中单独列为"文档与实际差异/待确认"，不得直接按文档断言把真实实现判为失败，除非该契约已被产品/研发确认。
- API full 执行中出现失败项时，必须全部进入接口缺陷清单；其中实际接口与文档、路由、方法或字段契约不一致的失败项，登记为"接口契约/文档一致性缺陷"或"接口契约/实现一致性缺陷"，不得因需要责任方决定修实现、修文档或调整契约而暂不登记缺陷。
- 没有接口文档时，必须通过前端 Network、页面操作、源码线索或自动探测形成"实测接口基线"，再基于实测行为设计用例和自动化断言。
- 被测服务未连通时，不得继续执行 API、UI、性能、安全专项；必须记录连接失败原因和后续未执行范围。
- 服务预检或 API smoke 中若发现服务级连接失败、读超时或真实认证不可用，不得继续执行 UI、性能、安全专项；API smoke 已通过后的 API 全量局部接口失败或超时，应记录为 API 缺陷/稳定性风险，允许 UI 按独立门禁执行，但不得继续性能、安全等会放大风险的专项。
- 自动化执行前必须先生成覆盖矩阵，逐条映射评审版用例到脚本、执行层级、执行状态；覆盖矩阵未生成时不得开始执行自动化。
- 不能只执行现有脚本就宣称完成自动化；必须说明评审版用例总数、已实现脚本数、已执行数、未实现数和阻塞数。
- 为减少 token 浪费，后续阶段不得重复读取明显已被最新摘要、覆盖矩阵或专项报告完整承接的信息。

## 输入输出约定
- 需求文档放在 `docs/requirements/`
- 原型与页面说明放在 `docs/prototype/`
- 接口文档放在 `docs/api/`
- 协议文档和报文样例放在 `docs/protocol/`
- 测试设备资料放在 `docs/device/`
- 需求分析报告输出到 `docs/analysis/`
- 测试方案与测试计划输出到 `docs/test-plan/`
- 测试用例输出到 `docs/cases/`
- 测试报告输出到 `docs/test-runs/YYYYMMDD-HHMMSS/reports/`（执行报告随批次归档）；`docs/reports/reference/` 存放方案、设计、排查记录等不随批次变化的参考资料。
- 缺陷清单输出到 `docs/test-runs/YYYYMMDD-HHMMSS/defects/`。
- 测试知识沉淀输出到 `docs/knowledge/`
- 自动化脚本输出到 `tests/`
- 每轮测试产出必须按测试时间归档到 `docs/test-runs/YYYYMMDD-HHMMSS/`，包含 `reports/`（执行报告+覆盖矩阵）、`defects/`（缺陷清单）、`raw/`（原始结果+run-logs）、`stage-status/`（阶段状态 JSON）和 `批次摘要.md`。
- `docs/reports/` 目录仅存放不随批次变化的参考资料（方案、设计、排查记录），不得存放执行报告和覆盖矩阵。
- `docs/defects/` 目录已废弃，缺陷清单统一存放在 `docs/test-runs/YYYYMMDD-HHMMSS/defects/`。
- 阶段门禁与统一执行入口参考 `docs/阶段执行检查清单.md`

## 评审规则
- 所有测试用例必须先经过评审，评审版才允许进入自动化执行。
- 评审优先识别遗漏、重复、歧义、不可执行项、错误预期。

## 自动化规则
- UI 自动化使用 Playwright。
- 接口自动化优先使用项目现有测试框架；没有则在 `tests/api/` 下建立最小结构。
- 性能测试使用 JMeter。
- 安全测试使用 ZAP。
- 性能测试执行前必须确认 JMeter 或 `JMETER_BIN` 已配置，压测参数已确认；否则写"未执行"。
- 安全测试执行前必须确认 Docker/ZAP 或本地 ZAP 执行入口已配置，扫描范围已确认；否则写"未执行"。
- 自动化执行后必须保存脚本、报告、缺陷清单。
- Windows 环境优先使用 `scripts/*.ps1` 执行入口。
- Linux/macOS 或 Git Bash 环境优先使用 `scripts/*.sh` 执行入口。
- 执行入口失败时，必须在报告中记录失败命令、失败原因和未执行范围。

## 自适应执行规则
所有执行类专项必须按"门禁检查 -> 冒烟执行 -> 结果分析 -> 下一步决策 -> 执行或停止 -> 沉淀配置"的闭环工作，不得只机械跑完整套脚本。

### 通用决策状态
| 状态 | 含义 | 后续动作 |
|------|------|----------|
| continue | 冒烟通过，风险可接受 | 继续执行下一层级或下一专项 |
| retest | 结果疑似偶发、环境波动或证据不足但门禁仍满足 | 原条件或缩小范围复测，并记录复测目的 |
| collect-evidence | 已发现问题但缺少定位证据 | 暂停加深测试，补充日志、监控、数据、配置或接口证据 |
| downgrade | 全量执行条件不足但可安全执行子集 | 降级为 smoke/partial/readonly/semi-auto |
| stop | 服务不可用、连接级失败、风险不可接受或授权不足 | 停止后续执行，写明未执行范围 |
| handoff | 缺陷需要研发/环境/产品确认 | 不做修复，只输出证据、影响和复测建议 |

### 冒烟后必须分析
- API 冒烟后必须判断服务连通、认证、核心只读接口是否稳定；连接级失败时停止 UI、性能、安全。
- UI 冒烟后必须判断登录、首页、一级菜单和核心页面是否可达；基础 UI 阻断时不得宣称 full-ui。
- 性能冒烟后必须判断错误率、P95/P99、超时类型、吞吐、恢复 smoke 和服务稳定性；未达标时由 AI 判断是 retest、collect-evidence、downgrade 还是 stop，默认不得直接加压。
- 安全基线冒烟后必须判断授权范围、认证上下文、目标稳定性和误伤风险；目标不稳定或授权不足时不得主动扫描。

### 下一步决策必须落盘
每个专项报告必须包含"下一步决策"或等价段落，至少写明：
- 决策状态：continue/retest/collect-evidence/downgrade/stop/handoff。
- 决策依据：引用具体报告、日志、指标、错误、覆盖矩阵或原始结果路径。
- 为什么这样做：说明继续或停止的理由。
- 下一步执行范围：继续执行哪些、复测哪些、补充哪些资料、哪些未执行。
- 对后续阶段影响：是否允许进入下一个专项或系统报告。

## 输出规范
每一步输出都必须按以下顺序组织：
1. 当前阶段
2. 输入文件
3. 产出文件
4. 正文/核心内容
5. 执行结论
6. 风险与待确认项
7. 下一步建议

说明：
- 正文/核心内容必须放在执行结论、风险与待确认项、下一步建议之前。
- 执行结论、风险与待确认项、下一步建议必须作为文档收尾部分，不得放在正文前面。
- 如某阶段没有额外正文，仍需保留上述顺序，可在正文/核心内容处写明"无额外正文"。

## 用例编号规范
- 所有测试用例表必须在"用例编号"前增加"序号"列。
- 序号从 1 开始连续递增，不得跳号；评审版用例也必须保留序号列。
- 用例编号仍保留业务类型前缀，例如 `API-AUTH-001`、`FUNC-AUTH-001`、`PERF-001`、`SEC-001`。

## 自动化分层规范
- smoke：登录、首页、核心导航、每个一级菜单可达性，作为最低闭环。
- readonly-regression：列表、筛选、详情、只读弹窗、导出响应等不改数据的回归。
- semi-auto：写操作入口、弹窗、必填校验，不提交真实数据。
- write-regression：新增、编辑、删除、处理、同步、修改密码等真实写操作；必须有专用测试数据和回滚策略。
- compatibility：浏览器兼容性，默认 Chromium，Firefox/Edge 按计划启用。
- full-ui/full-api 不等于"跑已有脚本"；只有覆盖矩阵中所有可自动化用例均已实现并按计划执行，才允许写"完整自动化已执行"。

## 阶段门禁
- 进入需求分析前，必须先列出实际读取的需求、原型、接口文档和/或实际项目地址；文档为空但项目地址可访问时，按项目地址建立实测基线；文档与项目地址均为空时只能输出"待确认"和"未执行"。
- 进入测试设计前，`docs/analysis/需求分析报告.md` 必须已生成且包含待确认项。
- 进入用例生成前，`docs/test-plan/测试方案.md` 和 `docs/test-plan/测试计划.md` 必须已生成。
- 进入自动化执行前，必须存在对应的 `docs/cases/*-评审版.md` 和 `docs/cases/测试用例评审记录.md`。
- 系统测试报告和测试知识沉淀采用"增量追加"模式：每个测试专项（API / UI / 性能 / 安全）完成后，必须立即增量更新 `docs/test-runs/YYYYMMDD-HHMMSS/reports/系统测试报告.md` 和 `docs/knowledge/` 对应文件，不得等待所有专项完成才统一生成。
- 系统测试报告中尚未执行的专项必须标注"未执行"并说明原因；已执行的专项必须记录结果、决策状态和证据入口；后续专项完成时追加到同一报告中。
- 每次增量更新系统测试报告后，必须同步更新 `docs/test-runs/YYYYMMDD-HHMMSS/批次摘要.md`。
- 知识沉淀不得修改历史报告和执行结果；只允许追加新的沉淀内容。
- 测试知识沉淀必须保存可复用业务规则、回归资产、缺陷模式、自动化维护项、环境工具问题和下一轮迭代建议；证据不足的信息必须写"待确认"。
- 每个专项完成后立即沉淀该专项的经验和模式，不等所有专项完成；沉淀范围与当前已完成的专项直接相关。
- 当 `run-full-test-flow.ps1` 执行完毕且 `10-system-test-report` 或 `11-knowledge-base` 状态不为 completed 时，AI 必须立即依次调用对应 skill（先 `system-test-report`，再 `knowledge-base`）生成或更新报告，不得跳过或等待下一轮。总控脚本已集成输出提醒机制，AI 应在检查执行结果时识别并响应该提醒。

## 调试与修复策略
- 发现输出数字不一致（如"失败数 N 但表格只有 M 行"）时，必须先隔离验证函数行为，不得直接怀疑数据源或重跑完整流程。
- 隔离验证步骤：`dot-source` 公共库 → 直接调用函数 → 检查返回值类型和 `.Count` → 确认是否符合预期。
- PowerShell 特别注意：单元素 `List[T]` 会被自动解包为 `T`，导致 `.Count` 返回对象属性数量而非集合元素数量；所有返回集合的函数必须用 `Write-Output -NoEnumerate` 或调用方用 `@()` 包装。
- 修复函数逻辑时，必须先审查同类函数是否存在相同问题，批量修复后再统一验证，不得逐个修逐个跑。
- 语法验证（AST 解析）通过后，先做函数级单元验证（dot-source + 直接调用），再跑完整流程；函数级验证已能暴露的问题不得等到流程执行才发现。
- 终端输出不可靠时（命令执行成功但无输出），优先用 `Write-Host` 显式输出或 `python -c` 替代隐式管道输出。
